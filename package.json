{
  "name": "better-bombuj-si",
  "private": true,
  "scripts": {
    "postinstall": "mkdir -p dist && cp ./manifest.json ./dist/manifest.json",
    "prebuild": "mkdir -p dist && cp ./manifest.json ./dist/manifest.json",
    "build": "tsc",
    "predev": "mkdir -p dist && cp ./manifest.json ./dist/manifest.json",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/chrome": "^0.2.2"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
